# CovenantPath (恩約) — Environment Variables
# Copy to .env.local — NEVER commit .env.local to git
#
# Supabase project: ukgolkaejlfhcqhudmve  (SHARED — CovenantPath uses `covenant` schema)
# Dashboard: https://supabase.com/dashboard/project/ukgolkaejlfhcqhudmve

# ── Supabase API  (Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL="https://ukgolkaejlfhcqhudmve.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."  # SERVER ONLY — never expose to browser

# ── Database  (Dashboard > Settings > Database > URI > Transaction Pooler, port 6543)
# search_path=covenant,auth,public is set in lib/db.ts — no need to add to URL
DATABASE_URL="postgresql://postgres.ukgolkaejlfhcqhudmve:[YOUR-DB-PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres"

# ── App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Production: NEXT_PUBLIC_APP_URL="https://covenantpath.vercel.app"

# ── Email (Resend — free: 100 emails/day)  https://resend.com
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="noreply@covenantpath.org"

# ── AI Matching (HuggingFace — free tier)  https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY="hf_xxxxxxxxxxxxxxxxxxxxx"
HUGGINGFACE_MODEL="Qwen/Qwen3-8B"

# ── Vercel CI/CD (set in GitHub repo secrets — not here)
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=
